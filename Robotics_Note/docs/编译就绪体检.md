# 编译就绪体检（发布前静态体检）

> 体检时间：2026-06-18 05:05（本环境**无 docker、未实际编译**，以下为只读静态分析）
> 范围：`book.tex` + `parts/**/*.tex`（29 个 .tex，含新建 `parts/appendix/matrix_calculus.tex`）；`refs.bib` + `docs/_pending_refs.bib`。
> 方法：grep/sort/uniq/awk 全量抽取 + Read 看上下文。**全程未改任何 .tex。**

## 总体结论

**没有发现"会让 LaTeX 跑挂（`\end{document}` 前致命报错）"的阻断项**：标签零重复、交叉引用零悬空、环境 `\begin/\end` 完全配平（3079/3079）。

**唯一一类真正需要修复才能让"参考文献正常显示"的问题是引用缺失**：有 **14 个 `\cite` key 在 `refs.bib` 与 `docs/_pending_refs.bib` 两处都查不到**（真缺，全部位于正在被并发编辑的 `planning_intro.tex`），外加 **22 个 key 只躺在 `docs/_pending_refs.bib`** 而该文件**并未被 `\addbibresource` 加入编译**。本书用 biblatex+biber（ElegantBook 默认），这些缺 key 不会让 LaTeX 主流程崩溃，但会渲染成加粗 **??** 并触发 biber 警告——视"发布质量"为阻断。

> 致命（让编译跑挂）问题计数：**0**
> 必须修复才能正常出参考文献：**缺 36 个 cite key 的来源**（14 真缺 + 22 待并入），见 §4 与文末清单。

---

## 编译拓扑（先厘清，关系到下面的判定）

`book.tex` 实际 `\input` 链（经 `part.tex` 逐层展开）只覆盖：

- front：`preface`、`notation`
- 正文：P0_intro→P0_math→P1_estimation→P2_slam→P3_planning→P4_control 各 `part.tex` 下列出的章
- 附录：**仅** `parts/appendix/style_gallery`

**两支 .tex 不在编译里**（无任何 `\input` 指向，已 grep 确认）：

- `parts/appendix/matrix_calculus.tex`（新建，51 个 label）——**未挂进 `book.tex`**
- `parts/P5_engineering/engineering_practice.tex`（1 个 label）——**未挂进 `book.tex`**，且 `parts/P5_engineering/part.tex`、`parts/appendix/part.tex` 都不存在

文献仅 `common/preamble.tex:57 \addbibresource{refs.bib}` 一处；`docs/_pending_refs.bib` **未加入**。`book.tex:39 \nocite{*}` 只会"引用所有已加入 .bib 里的条目"，对缺失 key 毫无补救作用。

---

## ① 重复 `\label`（致命）—— 无

- 全量 `\label{}`：**1605 个引用、1605 个唯一**（`grep -rhoE '\\label\{[^}]+\}' parts/ | sort | uniq -d` 输出为空）。
- 跨文件 / 同文件均**无重复**。`\cref` 不会因此歧义。

**结论：通过，无需处理。**

---

## ② 悬空 `\cref/\ref`（致命）—— 无

抽取了 `\cref`/`\Cref`/`\ref`/`\eqref`/`\autoref`/`\nameref`/`\labelcref`/`\cpageref` 及范围式 `\crefrange`/`\Crefrange`/`\cpagerefrange`（逗号分隔多目标已拆分、范围式两端 label 都抽）。

- 去重后被引用 label **1126 个**；与全量已定义 label 求差：**仅 1 个"差集"项 `...`**，来源是
  - `parts/appendix/style_gallery.tex:4` 行 `\verb|\cref{...}|`——在 `\verb` 内，是**说明文档的字面量，非真实引用**。**误报，忽略。**
- 排除该误报后：**真实悬空引用 = 0**。

**额外验证（关系到实际成书是否出现 ??）：**

- 只取**实际进入编译的文件**（剔除未挂进书的 `matrix_calculus.tex`、`engineering_practice.tex`）重算："built 文件里的引用全部能被 built 文件里的 label 满足"——**无一悬空**。即未挂进书的两支文件**不被任何成书章节引用**，不会在正式 PDF 里产生 `??`。
- `matrix_calculus.tex` 自身的 33 个引用：内部自洽；其指向**外部**的 6 个 label（`ch:lie`/`ch:nlopt`/`ch:rigid_body`/`ch:slam_est`/`eq:gn-normal`/`eq:nlopt-gls`）**均在已编译章节中确有定义**。故若日后把它 `\input` 进书，引用也能解析（见 §7 建议）。

**结论：通过，无需处理。**

---

## ③ 环境不配平（致命）—— 无

- 全局：`\begin{...}` **3079** 个 vs `\end{...}` **3079** 个，总数相等。
- **逐环境名**统计（自动覆盖所有出现过的环境名，不限题面列举的那批）：**无任何环境 begin≠end**。重点环境 codebox/derivation/insight/pitfall/practice/note/definition/theorem/proposition/lemma/corollary/equation/align(`*`)/cases/tabular/tikzpicture/enumerate/itemize/figure/table/proof/example/algo/paper 全部成对。
- 逐文件统计：**无任何文件出现某环境 begin≠end**。

> 说明：此法校验"计数配平"，不校验嵌套顺序；但配合"逐文件计数均匹配"，交错错配（如 `\begin{a}\begin{b}\end{a}\end{b}`）的可能性极低。

**结论：通过，无需处理。**

---

## ④ 缺失 `\cite` key（区分"真缺"与"待并入 _pending_refs"）

被引用的 cite key 去重 **154 个**；`refs.bib` 有 118 个，`docs/_pending_refs.bib` 有 22 个。

### ④-A 真缺：两处 bib 都没有（**必须补 bib 条目**）—— 14 个

全部出现在 **`parts/P3_planning/planning_intro.tex`**（该文件正在被并发编辑，见 §7）。biblatex 下渲染为加粗 **??** + biber `WARN: I didn't find a database entry for ...`。

| cite key | 文件:行号（首次/代表出现） |
|---|---|
| `dubins1957curves` | planning_intro.tex:994, :1310 |
| `gammell2014informed` | planning_intro.tex:957, :1309 |
| `gammell2020bit` | planning_intro.tex:973, :1309 |
| `harabor2011jps` | planning_intro.tex:543, :1307 |
| `janson2015fmt` | planning_intro.tex:970, :1309 |
| `kalakrishnan2011stomp` | planning_intro.tex:1172, :1311 |
| `lavalle2001kinodynamic` | planning_intro.tex:1057, :1310 |
| `likhachev2004ara` | planning_intro.tex:537, :1307 |
| `liu2017corridor` | planning_intro.tex:1165, :1311 |
| `mellinger2011minimum` | planning_intro.tex:1131, :1311 |
| `pivtoraiko2009lattice` | planning_intro.tex:1054, :1310 |
| `reeds1990optimal` | planning_intro.tex:1044, :1310 |
| `richter2016polynomial` | planning_intro.tex:1161, :1311 |
| `schulman2014trajopt` | planning_intro.tex:1172, :1311 |

**修复建议**：为这 14 个 key 在 `refs.bib` 补 `@inproceedings/@article` 条目（均为运动规划经典文献，题面/会议/年份在正文里已写明，可据此补全）。**因 `planning_intro.tex` 正被改写，建议待其定稿后再补，避免与并发改动撞车。**

### ④-B 待并入：只在 `docs/_pending_refs.bib`、且该文件未被 `\addbibresource` —— 22 个

这些 key 在正文里被 `\cite`，但 `docs/_pending_refs.bib` 是**待合并区**、**未挂进编译**，所以**当前同样会渲染成 ??**。属"条目已写好、只差并入"，修复成本低。

`agarwal2012ceres`、`alexa2003pointset`、`baker2004lk`、`bescos2018dynaslam`、`eade2017lie`、`engel2014lsd`、`engel2017dso`、`hanson2006visualizing`、`hartley2004multiview`、`lajoie2020doorslam`、`lowry2016vpr`、`magnus2019matrix`、`marton2009fast`、`newcombe2015dynamicfusion`、`nister2004fivepoint`、`petersen2012cookbook`、`runz2017cofusion`、`schmuck2019ccmslam`、`strasdat2012thesis`、`thrun2005probabilistic`、`tian2022kimeramulti`、`triggs2000ba`（共 22 个）

**修复建议（二选一）**：
1. 把 `docs/_pending_refs.bib` 的这些条目合并进 `refs.bib`（推荐，保持单一文献源）；或
2. 在 `common/preamble.tex` 追加 `\addbibresource{docs/_pending_refs.bib}`（快但留两个文献源，注意 latexmk 工作目录为项目根，路径用 `docs/_pending_refs.bib`）。

> 注：`magnus2019matrix`、`petersen2012cookbook`、`hanson2006visualizing` 等正是新章 `matrix_calculus.tex` 所引——若该章日后挂进书，更需把 _pending 并入。

---

## ⑤ 配平启发式告警（仅提示，经核查均为误报）

逐文件统计 `{` vs `}` 差、`$` 奇偶，仅 2 处触发，**核查后都是误报**：

| 文件 | 触发 | 核查结论 |
|---|---|---|
| `parts/P0_intro/intro_slam.tex` | 原始 `{` 比 `}` 多 1 | 计入转义括号后（`\{`×6 vs `\}`×5），**结构括号差=0**，配平正常。误报源于一处字面 `\{`/`\}` 不对称。 |
| `parts/P2_slam/loop_closure.tex` | `$` 计数为奇（925） | 无 `\$` 转义；奇数来自 **codebox `[text]`/`[bash]` 块里的字面 `$`**（如 :416 行 shell 提示符 `$ build/feature_training`）与 `\verb` 内字面美元。这些非数学定界符。**数学定界符成对，配平正常。** |

**结论：无需处理（均为 verbatim/转义导致的启发式假阳性）。**

---

## ⑥ 元叙事 / punt 残留（经核查均为误报）

`grep -rnE '详见|参见|见原书|见原文|不再赘述|此处略|从略|TODO|待补充|占位|worker|审稿|复检' parts/`，排除 `% ` 注释行与 `preface.tex`（其含反面示例）后，**仅 3 处命中，全部为正常正文 / 子串误伤**：

| 文件:行号 | 命中词 | 核查结论 |
|---|---|---|
| `parts/P2_slam/loop_closure.tex:732` | `复检`（来自"反**复检**测"） | 正文"避免反复检测同一回环"，子串误伤。 |
| `parts/P2_slam/loop_closure.tex:733` | `复检`（来自"反**复检**测"） | 正文"避免反复检测同一类回环"，子串误伤。 |
| `parts/P1_estimation/nonlinear_optimization.tex:1399` | `详见`（"详见下"） | 正文"破坏稀疏（**详见下**）"——指向紧邻下文的合法前向指引，非外书 punt。 |

- **无** `TODO`/`待补充`/`占位`/`worker`/`审稿`，**无** `详见原书`/`参见原文`/`不再赘述`/`此处略`/`从略` 式外部 punt。

**结论：无需处理。**

---

## ⑦ 并发编辑中、需事后复检的文件

体检瞬间（05:05）以下 **14 个 .tex 在最近 30 分钟内有改动**；其中 `control_intro.tex`(05:04:07)、`planning_intro.tex`(05:03:54)、`camera_calibration.tex`(05:01:49) 在体检前数十秒仍在变——**仍是移动靶，本报告结论需在它们定稿后复跑 §1–§4 复检**：

```
04:38  parts/P0_math/lie_theory.tex
04:40  parts/P1_estimation/nonlinear_optimization.tex
04:46  parts/P2_slam/visual_odometry.tex
04:47  parts/P0_math/rigid_body_motion.tex
04:47  parts/P2_slam/dense_mapping.tex
04:48  parts/P2_slam/camera_model.tex
04:51  parts/P2_slam/loop_closure.tex
04:51  parts/P2_slam/slam_system.tex
04:51  parts/P2_slam/lidar_slam.tex
04:53  parts/P0_intro/intro_slam.tex
04:54  parts/appendix/matrix_calculus.tex
05:01  parts/P2_slam/camera_calibration.tex   ← 临体检前仍在变
05:03  parts/P3_planning/planning_intro.tex   ← 临体检前仍在变；§4-A 的 14 个缺 key 全在此
05:04  parts/P4_control/control_intro.tex     ← 临体检前仍在变
```

> 上述所有"致命三项"（dup label / 悬空 ref / env 配平）已在**最新字节**上复跑一次确认通过；但因 planning_intro/control_intro/camera_calibration 仍在动，建议**它们停笔后再复跑一遍**本报告 §1–§4 的命令。

### 附：体检命令（可一键复跑复检）

```bash
cd /root/gpf/Robotics_Theory/Robotics_Note
# ① 重复 label
grep -rhoE '\\label\{[^}]+\}' parts/ | sort | uniq -d
# ③ 环境配平（per-env 失衡）
join -j1 -a1 -a2 -e 0 -o '0,1.2,2.2' \
  <(grep -rhoE '\\begin\{[^}]+\}' parts/|sed -E 's/.*\{([^}]+)\}/\1/'|sort|uniq -c|awk '{print $2,$1}'|sort) \
  <(grep -rhoE '\\end\{[^}]+\}'   parts/|sed -E 's/.*\{([^}]+)\}/\1/'|sort|uniq -c|awk '{print $2,$1}'|sort) \
  | awk '$2!=$3{print $1": begin="$2" end="$3}'
# ④ 缺 cite key（两处 bib 都没有）
comm -23 \
  <(grep -rhoE '\\[a-z]*cite[a-z]*\*?(\[[^]]*\])*\{[^}]*\}' parts/|grep -oE '\{[^}]*\}'|tr -d '{}'|tr ',' '\n'|sed 's/ //g'|grep -v '^$'|grep -vx '\*'|sort -u) \
  <(grep -hoE '^[[:space:]]*@[a-zA-Z]+\{[[:space:]]*[^, \t]+' refs.bib docs/_pending_refs.bib|sed -E 's/.*\{[[:space:]]*//'|sort -u)
```

---

## 文末：必须修复才能编译/正常发布的清单

> 严格意义上**没有让 LaTeX 主流程崩溃的项**（致命三项全通过）。以下是"必须修复才能让参考文献正常显示、达到发布质量"的项：

1. **【真缺 14 条文献】** 为 §4-A 列出的 14 个 cite key 在 `refs.bib` 补条目（全在 `planning_intro.tex`，建议待该文件定稿后补）。否则正文 14 处引用显示 **??**。
2. **【并入 22 条文献】** 把 `docs/_pending_refs.bib` 的 22 个条目并入 `refs.bib`，**或**在 `common/preamble.tex` 加 `\addbibresource{docs/_pending_refs.bib}`。否则正文这 22 处引用同样显示 **??**。
3. **【确认两支游离 .tex 的归属】**（非阻断，但发布前需决断）：`parts/appendix/matrix_calculus.tex`（新建，51 label）与 `parts/P5_engineering/engineering_practice.tex` 当前**未挂进 `book.tex`**。若要收录：在 `book.tex` 的 `\appendix` 段加 `\input{parts/appendix/matrix_calculus}`（其外部引用经验证可解析，但记得先完成第 2 项把 _pending 文献并入，否则该章引用会出 ??）；若不收录则保持现状、忽略其内部 label。
4. **【定稿后复检】** planning_intro / control_intro / camera_calibration 等正在并发编辑的文件停笔后，复跑上方"附：体检命令"再确认一次 §1–§4。
