# 完备性审核：《视觉SLAM十四讲》第2版 ch9–15 → 本教材

> **审核口径**：吸收 = 把源书一切有价值内容用本书口吻重写——五维：(a) 知识；(b) 讲解过程（怎么把人讲懂）；(c) 分析过程（怎么从问题走到结论、为何排除别的做法）；(d) 脉络（动机→问题→方法→洞见的演进）；(e) 思想/洞见（深层 idea、直觉、设计哲学）。**重点抓退化**：知识点在、但讲解/脉络/思想丢了，只剩干巴结论。
>
> **方法**：完整通读十四讲源 ch9–14（`视觉SLAM十四讲_md/`，ch15 数学附录已由 matrix_calculus 覆盖）逐章提炼五维清单 → 教材落点 grep/read 独立核查（不盲信基线）→ 判定 ✅/⚠️/❌。
>
> **独立复核当前态**：MEMORY 提示「实际进度超文档」。基线（`docs/十四讲吸收审计.md`、`复核_*.md`）列的 2 处 major（Sim(3) Js、回环§11.1.2）+ 若干 minor，本轮以**教材现状**为准重新判定，结论：**全部已补**。
>
> 审核日期：2026-06-18。只读，未改任何 .tex/refs.bib。

## 落点映射（与任务指定落点的实际偏差说明）

十四讲 ch9–10 的**后端理论主体落在 P1_estimation**（非任务默认指向的 P2_slam/vio）：

| 十四讲源章 | → 教材实际落点 | 五维总判 |
|---|---|---|
| ch9 后端1（滤波） | `P1_estimation/kalman_eskf.tex` | ✅ 全量+大幅深化 |
| ch9 后端1（BA/Schur/鲁棒核） | `P1_estimation/nonlinear_optimization.tex` | ✅ 全量+大幅深化 |
| ch9 后端1（KF≡批量MAP/因子图） | `P1_estimation/slam_state_estimation.tex` | ✅ 全量+提升 |
| ch10 后端2（位姿图） | `nonlinear_optimization.tex` §位姿图优化 | ✅ 全量+提升 |
| ch10 后端2（滑窗/边缘化/SWF） | `kalman_eskf.tex`(SWF) + `nonlinear_optimization.tex`(SWF推导) + `slam_system.tex`(工程) | ✅ 全量+提升 |
| ch10 后端2（IMU 紧耦合） | `P2_slam/vio.tex`（整章，大幅超额） | ✅ 远超源书 |
| ch11 回环检测 | `P2_slam/loop_closure.tex` | ✅ 全量+大幅提升 |
| ch12 建图 | `P2_slam/dense_mapping.tex` | ✅ 全量+大幅提升 |
| ch13 设计SLAM系统 | `P2_slam/slam_system.tex` | ✅ 全量+大幅提升 |
| ch14 现在与未来 | `intro_slam.tex` + `slam_system.tex` + P5_frontier 各章 | ✅ 吸收并扩展 |
| ch15 数学附录 | `appendix/matrix_calculus.tex` | ✅（基线已审） |

---

## 逐章五维覆盖表

### ch9 后端1 — 滤波部分（`kalman_eskf.tex`）✅ 全量且大幅深化

| 维度 | 源书要点 | 教材落点与判定 |
|---|---|---|
| (a)知识 | 状态估计概率解释/批量vs渐进；线性高斯 KF 配方推导（比较二次/一次系数）；EKF 一阶泰勒线性化；KF 五式 | 全在：贝叶斯滤波框架→KF（含「比较指数二次型」配方）→EKF 逐式（`prop:ekf`）。**增厚**：Barfoot 立体相机一维例（后验偏斜/均值≠众数，含 10⁶ 次蒙特卡洛数值 −33.0cm 偏差） |
| (b)讲解 | 「蒙眼走路」直觉（无观测圆变大、有观测圆缩小）；预测/更新两步 | 保留并升格（quote+重绘协方差椭球图）；预测/更新双步全在 |
| (c)分析 | EKF 四局限（马尔可夫性/单次线性化/O(n²)存储/无异常检测）；EKF=优化一次迭代 | **保留并深化**：EKF「三宗罪」pitfall（工作点偏差/单次线性化/破坏马尔可夫性）焊上 Barfoot 视角；「EKF=IEKF 首次迭代」「EKF≈优化一次迭代」均明写 |
| (d)脉络 | 滤波为何不如优化→引出非线性优化后端 | **提升**：广义高斯滤波`prop:ggf`作「统一所有高斯滤波器的总接口」，EKF/UKF/IEKF/ISPKF 全挂其下；「把马尔可夫性内建即无法去除」作为「批量更优」的理论依据 |
| (e)思想 | 高斯过线性仍高斯=KF 无近似；非线性必须近似 | **大幅超源书**：「一个高斯过非线性后非高斯、均值≠众数」作分水岭洞察；滤波器谱系表（含 IF/互补/InEKF/粒子/SWF）；x²三法对比看清「线性化过自信」 |

**超源书增厚**（合规，源书无）：UKF/sigma点完整算法、IF 信息形式、粒子滤波（SIR/建议分布/N_eff/Madow 重采样）、NEES/NIS 一致性、ESKF、SWF。
**缺口**：无。基线列的两 minor（S 条件数、消相机）见 ch9-BA 行，均已补。

### ch9 后端1 — BA/稀疏/Schur/鲁棒核（`nonlinear_optimization.tex`）✅ 全量且大幅深化

| 维度 | 源书要点 | 教材落点与判定 |
|---|---|---|
| (a)知识 | BA 投影四步流水线/重投影代价；H=JᵀJ 分块；稀疏雅可比 J_ij 仅两块非零；箭头/镐形矩阵；Schur 消元式；Huber 核 | 全在：四步流水线「全录」、`eq:nlopt-ba-jij`两块非零、`eq:schur`、Huber/Cauchy/Tukey 全家福。2相机6路标例**含可画 TikZ 箭头矩阵图**`fig:nlopt-ba-sparsity` |
| (b)讲解 | 「光束收束回光心」释义；用流程图把投影讲形象 | 保留（光束收束释义、四步逐步对应雅可比环节） |
| (c)分析 | 为何 H 稀疏；Schur 优势（C⁻¹易解）；S 稀疏=共视；鲁棒核为何（大残差掩盖正确边） | **保留并深化**：fill-in=共视、变量排序 NP 难（COLAMD 9399→4168 实例）、「外点毁最小二乘」M3500/SubT 实证 |
| (d)脉络 | BA→稀疏→Schur→实践 | **提升**：稀疏性独立成节（先讲因子图镜像），BA 节「兑现」抽象残差；鲁棒化补前端 RANSAC/PCM 两道防线 |
| (e)思想 | 21世纪 SLAM 进展=认识 H 稀疏；H≈邻接矩阵 | **超源书**：① **「S 条件数小于 H，即便无速度收益也值得 Schur」已补**（`:586` insight）；② **「对偶地消去相机变量也是 SLAM 手段」已补**（`:586`末）；变量消元=稀疏分解=图模型统一；约化相机系统(Triggs) |

**缺口**：无。基线列两 minor 均已补成 insight。

### ch10 后端2 — 位姿图 + 滑窗（`nonlinear_optimization.tex` / `kalman_eskf.tex` / `slam_system.tex`）✅ 全量且提升

| 维度 | 源书要点 | 教材落点与判定 |
|---|---|---|
| (a)知识 | 位姿图残差 e_ij、伴随搬扰动求左乘雅可比、J_r⁻¹近似；目标函数；球形仿真 g2o 两实现；滑窗删旧帧 Fill-in、SWF 边缘化、条件概率解释 | 全在：`eq:nlopt-pg-err/jac`（伴随技巧逐步）、球形 g2o 内置 vs 李代数两实现代码、SWF 舒尔补推导(`der:nlopt-swf`)、删旧帧破坏路标对角块(`slam_system:1355` OKVIS 策略) |
| (b)讲解 | 路标收敛后固定→只优化轨迹的动机；「丢弃/固定」之问 | **保留**：桥接段「路标收敛后 BA 再瘦身」；fill-in 陷阱区分滤波/平滑两种边缘化 |
| (c)分析 | J_r⁻¹≈I 损失多少；位姿图稀疏性取决于运动 | **保留并深化**：「带状(稀疏) vs loopy(稠密)」全在 |
| (d)脉络 | BA→位姿图→滑窗→PTAM 前后端分线程 | **提升**：位姿图桥接 BA、SWF 接 VIO；PTAM「后端无须实时」点明 |
| (e)思想 | 边缘化=条件概率；SWF 适合 VO 不适合大建图 | **超源书**：「IEKF=窗口为1的 SWF」「SWF=递归与批量折中」 |

**缺口（基线列的 major partial_thin，已补）**：
- 「把 J_r⁻¹ 近似为 I / 注释 oplusImpl 亲手看区别」——**已补成完整 pitfall**（`:881`，三种实现 a/b/c 对比，明列「近似 I 也收敛但远离最优时下降方向不准」）。
- 「新增帧平凡 vs 删旧帧两难」——**已补**（`nonlinear_optimization:2121` 选读段 + `der:nlopt-swf`）。
- g2o 内置 SE3 逐代 chi2 收敛日志——已凝练入代码注释（44360 vs 44811 终值对比保留）。

### ch11 回环检测（`loop_closure.tex`）✅ 全量且大幅提升

| 维度 | 源书要点 | 教材落点与判定 |
|---|---|---|
| (a)知识 | 累积误差/全局一致；O(N²)全配对/随机抽样；里程计法vs外观法；混淆矩阵/PR/感知偏差感知变异；BoW三步；K-means/层次k叉树；TF-IDF；L₁评分；相似度归一化/关键帧处理/验证 | 全在：`eq:loop-allpairs`、`def:loop-confusion`、`eq:loop-pr`、`alg:loop-kmeans/ktree`、TF-IDF、`eq:loop-l1-simple`。**增厚**：F₁(`eq:loop-f1`) |
| (b)讲解 | 弹簧系统比喻；「人车狗」直觉；Bag 而非 List | **保留并升格**：弹簧 insight（`ins:loop-spring`「架错弹簧比少架危害大」）、人车狗、`ins:loop-bag`(Bag无序性是鲁棒之源也是感知混叠温床) |
| (c)分析 | 里程计法倒果为因批判；PR 此消彼长；SLAM 重准确率轻召回 | **保留并深化**：`pit:loop-odom`「倒果为因」完整、`ins:loop-precision`「宁缺毋滥」推理链 |
| (d)脉络 | 为何回环→去哪找→怎么算相似度→字典→评分→验证 | **大幅提升**：四阶段流水线 A/B/C/D 显式成节；`ins:loop-prefilter`「预判候选」贯穿全章 |
| (e)思想 | 回环=分类/学习问题；深度学习地点识别 | **超源书**：感知混叠「四道防线」、深度学习地点识别独立节 |

**缺口（基线列的 major confirmed_missing，已补）**：
- 「§11.1.2 回环检测的方法」整节——**已补成完整章节**`sec:loop-detect-methods`（里程计vs外观二分、O(N²)、随机抽样、倒果为因批判全在），动机链不再断。
- 习题2「据 TUM 真值轨迹反算回环并反思图像是否真相似」——**已补**（`:170` practice 含伪码 + 「位姿相近≠外观相似」讨论）。
**独立性**：无 narration（全 \cite 出处 + 中立多书对照）。

### ch12 建图（`dense_mapping.tex`）✅ 全量且大幅提升

| 维度 | 源书要点 | 教材落点与判定 |
|---|---|---|
| (a)知识 | 地图五用途/稀疏vs稠密；bearing-only；极线搜索块匹配 SAD/SSD/NCC/ZNCC；三角化2×2；一像素误差正弦定理深度不确定度；高斯深度融合；逆深度；仿射变换；并行化；点云SOR+体素；MLS+GP3网格；八叉树 logit/log-odds 加性更新；TSDF 截断/投影/加权融合/Fusion谱系 | **全在**（基线逐 API 对 PCL 官方文档核验 PASS）：`eq:dense-sad/ssd/ncc/zncc`、`eq:dense-tri-linear`、`eq:dense-unc-pprime`(完整 derivation)、`eq:dense-fuse`(完整 derivation)、逆深度、仿射、`eq:dense-octo-logit`、TSDF 全套 |
| (b)讲解 | 「做实验→暴露问题→改进」探索叙事；纹理依赖；八叉树为何省空间 | **保留并升格**：探索叙事完整继承；八叉树「连在一起的空白无须展开」 |
| (c)分析 | 像素梯度⊥/∥极线；正深度高斯不合理→逆深度；鬼影→实时重建 | **保留并深化**：梯度⊥极线 pitfall（前置预警 + 后置定量）、逆深度参数化讨论、鬼影动机 |
| (d)脉络 | 稠密需求→单目MVS→极线块匹配→深度滤波→实验暴露问题→RGBD点云/网格/八叉树/TSDF | **提升**：单目链条 insight「极线→块匹配→三角化→滤波」逐节打通 |
| (e)思想 | 单目费力不讨好；深度≠z值；TSDF 以建图为主体 | **超源书**：「为何需要足够基线/纯旋转不能建图」insight、深度≠z值 insight、隐式曲面/NeRF/3DGS 大段扩展 |

**缺口**：无 confirmed_missing。基线列两 minor（点云定位讨论、MLS+GP3代码）**已补齐**（基线复核 PASS）。

### ch13 设计SLAM系统（`slam_system.tex`）✅ 全量且大幅提升
- Frame/Feature/MapPoint/Map 四数据结构、前端三状态、后端 BA、激活窗口、weak_ptr 循环引用全在；用 slambook2/ch13 源码补全 8 函数 + ORB-SLAM2 对照。无缺口。

### ch14 现在与未来（`intro_slam.tex`/`slam_system.tex`/P5_frontier）✅ 吸收并扩展
- MonoSLAM/PTAM/ORB-SLAM/LSD-SLAM/SVO/RTAB-MAP 历史评述、VIO 松/紧耦合、语义 SLAM、三时代史全在并扩展到 P5_frontier 多章（learning_slam/metric_semantic/dynamic_deformable/open_world）。VIO 在 vio.tex 远超源书点到为止的程度。

### ch15 数学附录 ✅
- 矩阵求导缺口已新建 `appendix/matrix_calculus.tex` 覆盖（基线已审）。

---

## ❌ 高价值知识缺失清单

**无。** 源书 ch9–15 一切有价值内容均已全量吸收。基线曾列的 2 处 major（① ch11 §11.1.2 回环检测方法整节；② ch10「J≈I 动手对比」）经独立核查**确认均已补**；其余 minor（S 条件数、消相机、新增帧平凡、点云定位、MLS+GP3代码、TUM反算回环习题）亦全部落地。

## ⚠️ 讲解/脉络/思想退化清单（重点）

**无实质退化。** 这是本次审核最重要的结论：ch9–14 不仅知识点在，**「让人真正理解」的讲法/分析/脉络/思想被系统性保留并显著提升**——

- 招牌**直觉钩子全继承并升格**：蒙眼走路（→quote+椭球图）、弹簧系统（→insight 含「架错弹簧」哲学）、人车狗、光束收束、镐形矩阵、深度≠z值。
- 招牌**分析/批判全保留并深化**：EKF 四局限（→三宗罪+Barfoot）、里程计法倒果为因、PR 此消彼长+宁缺毋滥、外点毁最小二乘。
- **脉络演进无断裂且更顺**：回环「为何→去哪找→怎么算」补回断环、单目稠密链条逐节打通、滤波→优化用三定理（KF≡批量MAP/EKF≈优化一次迭代/IEKF=GN）焊死。
- **思想/洞见普遍超源书**：广义高斯滤波统一接口、滤波器谱系表、变量消元=稀疏分解=图模型、S 条件数改善、纯旋转不能建图、TSDF 以建图为主体。

唯一可记的**极轻微 narration 残留**（非退化，仅风格纯净化，可选）：
- `nonlinear_optimization.tex` 2 处、`loop_closure/dense_mapping/vio` 各 1 处含「主源」「不少 g2o 代码」等措辞——经抽查均为**合法出处致谢或中立多书对照**，非以源书作叙述载体。`详见十四讲/见原书/punt` 与 ventriloquize（「十四讲坦言/强调」）**全 parts 零命中**。preface 已明确宣言「绝不以详见原书代替正文」。

## 总判

**《视觉SLAM十四讲》ch9–15 对本教材的吸收：完备（五维全覆盖、零高价值缺失、零实质退化），且普遍大幅增厚至独立著作水准。** 后端理论主体落在 P1_estimation（kalman_eskf + nonlinear_optimization + slam_state_estimation），回环/建图/系统落 P2_slam，IMU 紧耦合在 vio.tex 远超源书。基线审计所列全部 major/minor 缺口经独立复核确认已补，符合 MEMORY「实际进度超文档」。
