# 第4章《值迭代与策略迭代》中文 OCR MD 保真度审核

## 概述

以英文权威版（`/tmp/rlref_en/ch4.txt`，必要处核对原版 PDF）为数学/内容真值，逐节比对中文结构化 MD
（`强化学习的数学原理_分章/04_第4章_值迭代与策略迭代.md`）。

总体结论：中文 MD 的**数学公式整体保真度高**，主要正文公式、伪代码、q-table 数值、示例数值均与英文一致，未发现影响结论的数值/符号实质错误。问题集中在
**两处公式 OCR 错位（下标重复、收敛箭头错位）** 与**多处英文有而中文缺的解释性句子/命名/定义**（漏识）。下表逐条列出。

注：英文提取为非结构化 pdftotext，夹杂页码/断行/图表错位，已忽略此类噪声，仅比实质内容与数学。

## 问题清单

| 位置[节/公式] | 类型 | 中文MD现状 | 英文正确形式 | 建议 |
|---|---|---|---|---|
| 4.1.2 示例 / 行84 | 漏识 | "...其中有一个禁止区域和一个目标区域。参数设置为 r_boundary=-1,..." | 英文明确给出 "The target area is s4."（目标区域是 s4） | 补"目标区域为 s4"，与图4.2/q-table 一致 |
| 4.1 / 行44（v_k 是否状态值） | 漏识 | "...当 k 有限时，v_k 可能并不满足任何一个贝尔曼方程，因此也不是某一个策略的状态值。" | 英文(行202-207)还补充：举例 "it does not satisfy v_k=r_{π_{k+1}}+γP_{π_{k+1}}v_k 或 v_k=r_{π_k}+γP_{π_k}v_k in general"，并补 "由于 v_k 不是状态值，q_k 也不是动作值(action value)" | 补这两句，尤其"q_k 不是动作值"这一明确论断 |
| 4.1.1 / 式(4.2)后，行62 | 漏识 | "...如果有多个动作的值都相同并且最大，那么可以选择其中任意一个动作。" | 英文(行158-161)补：选任一动作"不影响算法收敛性(without affecting the convergence)"，并指出"由于新策略 π_{k+1} 选取最大 q_k 的动作，这种策略称为贪婪(greedy)策略" | 补"不影响收敛"和**贪婪策略(greedy)**的命名，后文4.2.2也用到 greedy |
| 4.2.2 / 式(4.2型解)，行312 | 漏识 | 解给出后直接"上述算法的伪代码参见算法4.2" | 英文(行701)称该解为 "the greedy optimal policy"（贪婪最优策略） | 与上条一致，补"贪婪"命名 |
| 方框4.1 证明 / 行253 | 公式不符(弱化) | "...因为 γ^n→0 且 P_{π_{k+1}}^n 的每一个元素都在[0,1]区间。" | 英文(行529-531)："γ^n→0 且 P_{π_{k+1}}^n 是非负随机矩阵(nonnegative stochastic matrix)；随机矩阵指各行行和为1的非负矩阵" | 中文用"元素∈[0,1]"替代了"随机矩阵(行和为1)"，且整段**随机矩阵定义缺失**；建议改为"非负随机矩阵"并补定义 |
| 方框4.1 证明 / 式 行250 | OCR错(下标重复) | `... ≤ γ^{n} P_{π_{π_{k+1}}}^{n} (v_{π_k}-v_{π_{k+1}})` 出现重复下标 `P_{π_{π_{k+1}}}` | 应为 `γ^{n} P_{π_{k+1}}^{n} (...)`（单层下标，见英文行524） | 删去多余的一层 `\pi_`，改为 `P_{\pi_{k+1}}^{n}` |
| 4.2.1 / 行267（定理4.1说明） | 公式不符(表述偏差) | "...值迭代的收敛性可以推出策略迭代的收敛性，换句话说，策略迭代比值迭代的收敛性更强。" | 英文(行548-551)："若两算法从相同初值出发，由于策略评价步骤中嵌入的额外迭代，策略迭代比值迭代**收敛更快(converge faster)**" | 英文强调"收敛更快"且有"相同初值"前提；中文"收敛性更强"语义偏差，建议按英文改为"收敛更快" |
| 方框4.2 / 行284 第4行 | OCR错(撇号位置) | 该步引入 `π_k'`，中文写作 `r_{π_k^{\prime}}` 与 `P_{π_k^{\prime}}`（撇号在 k 上：`π_{k}'`） | 英文记号为 `π_k'`（撇号作用于整个 π_k，行581 `r_{π_k'}`），与后文行287 `P_{π_k'}` 一致 | 记号本身一致，但确认全章 `π_k'` 撇号位置统一（行284一处 `\pi_{k}^{\prime}` 与行287 `\pi_k'` 应同一对象）；非数值错，属一致性提醒 |
| 4.3.1 / 截断迭代链 行538、545 | OCR错(箭头/标注错位) | "值迭代算法←v_1←—" 与 "截断策略迭代算法←\bar v_1←-" 的多行对齐被打乱，行545 出现 `\bar{v}_1\gets -` 等错位 | 英文(行1815-1839)为规整的三分支：run once→v_1(值迭代)、finite→\bar v_1(截断)、∞→v_{π_1}(策略迭代) | 内容齐全但排版/箭头错位，建议重排为清晰三分支对齐，确保 `\bar v_1` 对应截断 |
| 4.3.2 命题4.1 / 行569 | 漏识(命题名) | "命题4.1 在截断策略迭代算法中，嵌套在其策略评价步骤中的迭代算法为..." | 英文(行1924)命题有标题 "Proposition 4.1 (**Value improvement**)"（值改进） | 补命题命名"（值改进）" |
| 4.3.2 / 行598 | OCR错(下标 k vs k-1) | "该命题需要条件 v_{π_k}^{(0)} = v_{π_{k-1}}" 正确；但同句"因为无法获得 v_{π_{k-1}} 而只能得到 v_{k-1}" | 英文(行2014-2015)一致：v_{π_{k-1}} 不可得、仅 v_{k-1} 可得 | 此处正确，仅核对无误（无需改） |
| 4.2.3 / 行330 一个简单例子 | OCR错(版面割裂) | "参数设置为 r_boundary=" 后断到 "(b)" 再到 "-1, r_target=1, γ=0.9" | 英文(行721-722)：r_boundary=-1, r_target=1, γ=0.9（数值正确） | 数值正确，仅图标记"(b)"插入正文造成断裂，建议清理版面 |
| 4.2.1 / 行203（解析解） | 核对一致 | "v_{π_k} = (I-γP_{π_k})^{-1} r_{π_k}" | 英文(行469)同 | 一致，无需改 |
| 算法4.1 目标 / 行88 | 漏识(目的短语) | "目标：求解最优状态值和最优策略。" | 英文(行220-221)："...optimal policy **for solving the Bellman optimality equation**" | 可补"以求解贝尔曼最优方程"（次要） |
| 4.5 问答 / 行668 | OCR错(列表符缺失) | 该条"提问：...运行多少次迭代？回答：..."缺少行首 ◇ 项目符且问答挤在同一行 | 英文为独立 Q/A 条目 | 补 ◇ 并拆分问/答（格式，非数学） |

## 问题计数

- 合计记录 **14** 行；其中**实质问题 12 项**（OCR错 4：行250 下标重复、行538/545 箭头错位、行330 版面割裂、行668 列表符缺失；漏识 6：目标 s4、q_k 非动作值、greedy 命名×2、随机矩阵定义、命题"值改进"命名、算法4.1目的短语；表述偏差/弱化 2：行253 随机矩阵→[0,1]、行267 "收敛更强"→应"收敛更快"），另有 2 项为"核对一致/一致性提醒"不计为缺陷。
- **未发现任何影响结论的数值错误**：式(4.1)–(4.5)、表4.1–4.6 数值、两示例迭代值（-1, -1.9, -2.71, -1.71；-10/-9；Table4.5）均与英文一致。

> 最重要的两处：①行250 公式 `P_{π_{π_{k+1}}}^n` 多一层下标（确为 OCR 错）；②多处"greedy/贪婪"命名与"q_k 不是动作值""随机矩阵定义"等解释性内容漏识。
