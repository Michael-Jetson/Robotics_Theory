# 第8章《值函数方法》中文 OCR MD 保真度审核报告

## 概述

- **审核对象**：`强化学习的数学原理_分章/08_第8章_值函数方法.md`（中文结构化 OCR 版）
- **真值基准**：`Book-MathFoundationRL-en/3 - Chapter 8 Value Function Methods.pdf`（英文权威版，pdftotext 提取于 `/tmp/rlref_en/ch8.txt`）
- **审核范围**：8.1~8.6 全章逐节比对（含方框 8.1~8.6、算法 8.1~8.4、Q&A）
- **结论概要**：整体内容完整、数学推导逐式齐全，无大段漏识。发现 1 处实质性事实错误（状态值/动作值混淆）、1 处公式漏转置（OCR）、若干 OCR 排版瑕疵（双 \tag、Φ 加 bar、字体不一致），以及一处贯穿后半章的公式编号系统性偏移（+1）。另有 2 处中文添加了英文原文没有的解释性内容（非 OCR 错，列为内容不符供核对）。

---

## 问题清单

| 位置[节/公式] | 类型 | 中文 MD 现状 | 英文正确形式 | 建议 |
|---|---|---|---|---|
| 8.1，式(8.1)后的最小二乘解（MD 第113行） | OCR错/公式不符 | `w^* = (Φ^T Φ)^{-1} Φ v_π`（第二个 Φ 缺转置） | `w∗ = (ΦᵀΦ)⁻¹ Φᵀ vπ`（英文 ch8.txt 第341行） | 补回转置，改为 `(\Phi^T\Phi)^{-1}\Phi^T v_\pi` |
| 8.3.1（MD 第969行） | 公式不符/事实错 | "算法(8.36)只用来估计状态值，即做策略评价" | 英文(行1930-1932)："The value estimation step in (8.35)…accurately estimating the **action values** of a given policy requires (8.35)…" | "状态值" 应改为 "**动作值**"；该式(中文8.36/英文8.35)估计的是 q 值 |
| 8.2.5，式(8.20)（MD 第594行） | OCR错 | 矩阵写作 `\bar{\Phi}`（Φ 上加横线），但正文与后续(8.21)(8.25)(8.26)均用无横线 `Φ` | 英文(行1227)为普通 `Φ`（`Φ = [… φᵀ(s) …]`） | 去掉 `\bar`，统一为 `\Phi`，避免与后文符号不一致 |
| 8.2.5，式(8.27)（MD 第708行） | OCR错（排版） | 行尾出现 `\tag {8.}\tag{8.27}`（多余空 tag） | 仅应有 `(8.27)` 一个编号（英文行1464） | 删除多余的 `\tag {8.}` |
| 8.2.5 起 公式编号（MD 第817/836/850/860/922/958/998/1041/1063行） | 公式不符（编号偏移） | 中文把方框8.5 中 `Φw = Φ(ΦᵀDΦ)⁻¹ΦᵀD(rπ+γPπΦw)` 额外编号为 (8.32)，致后续整体 +1：中文(8.33)误差界、(8.34)、(8.35)LSTD、(8.36)Sarsa、(8.37)Q-learning、(8.38)DQN目标、(8.39)DQN梯度 | 英文该式**无编号**；英文对应编号为：误差界(8.32)、Box8.6首式(8.33)、LSTD(8.34)、Sarsa(8.35)、Q-learning(8.36)、DQN目标(8.37)、DQN梯度(8.38) | 中文内部交叉引用尚自洽，但与英文版及其他章引用不一致；建议删去(8.32)这一多余编号，使后半章编号回到与原书一致 |
| 8.2.5 方框8.4（MD 第715行） | 内容不符（添加） | 增句："一个常见的非对称正定矩阵就是旋转角度小于90度的旋转矩阵，感兴趣的读者可以自己思考一下原因。" | 英文原文无此句（grep 全文无 rotation/90 度相关内容） | 系译者补注，非原书内容；若须严格对齐原书可删，或标注为译者注 |
| 8.6 Q&A "什么是平稳分布"（MD 第1230行） | 内容不符（添加+漏译） | 增句："虽然该概念非常基础和重要，但是它通常不会出现在算法表达式中，因此大部分读者只需要知道这个概念的存在就足够了。" | 英文(行2645-2649)对应处为："The stationary distribution is important not only for the value approximation method but also for the **policy gradient method**, which will be introduced in Chapter 9." | 英文强调"对第9章策略梯度同样重要"被替换/弱化；建议核对，按原书补回"对策略梯度方法也重要"之意，并酌情删去添加句 |
| 8.4.1 经验回放（MD 第1080行） | 内容不符（添加） | 增句："如果我们没有对采样过程的先验知识，那么最简单的方法是假设它是均匀分布的。" | 英文(行2252-2253)："The simplest way to describe the distribution of the state-action pair (S, A) is to assume it to be uniformly distributed."（无"先验知识"前提） | 添加了原文没有的"无先验知识"条件；语义略有偏移，建议按原文表述（最简单方式=假设均匀分布） |
| 8.2.5 式(8.20)/(8.21) 与方框8.1 一致性（MD 第594/745行） | OCR错（字体不一致，轻微） | `\mathbf{1}_n` 与 `\mathbb{1}_n` 混用（如第742、745行用 `\mathbb{1}_n`，第736行用 `\mathbf{1}_n`） | 英文统一为 `1_n`（黑体一） | 统一为 `\mathbf{1}_n`；非数学错误，属排版一致性 |

---

## 说明（已核对为"非问题"的项）

- 8.2.3 节中文"目前最常见的是使用人工神经网络…下面重点介绍…线性函数"相对英文"There are two ways…first…second…"是行文重写，数学/内容等价，不计为缺陷。
- 蒙特卡罗法(MD 第328行)未抄录英文显式回合记号 `(s0,r1,s1,r2,…)`，但叙述完整，内容无误，不计为漏识。
- 算法 8.2/8.3/8.4 的伪代码、方框 8.3/8.5/8.6 的逐式推导、式(8.6)~(8.31) 主体、Jensen 不等式步骤等，均与英文逐式一致，无错。
- 图 8.5 中 `Pπᵀ` 数值矩阵、特征值 `{−0.0449, 0.3, 0.4449, 1}`、`dπ=[0.0345,0.1084,0.1330,0.7241]ᵀ`、右特征向量 `[0.0463,0.1455,0.1785,0.9720]` 等数值，与英文完全一致。
- 各图/算法参数（γ=0.9, ε=0.1, r_boundary=r_forbidden=−10, r_target=1, α=0.001；批量 100/50；1000 步 vs 100000 步 等）均与英文一致。

---

## 问题计数

**实质性问题：8 处**
（其中 OCR/公式类 4 处：(8.1)后 LS 漏转置、状态值/动作值错、式(8.20) Φ 加 bar、式(8.27) 双 tag；编号系统性偏移 1 处；内容不符/添加 3 处：方框8.4 旋转矩阵句、Q&A 平稳分布句替换、经验回放"先验知识"句）
另含轻微排版一致性问题 1 处（`\mathbf{1}` 与 `\mathbb{1}` 混用），合计 **9 项**。
