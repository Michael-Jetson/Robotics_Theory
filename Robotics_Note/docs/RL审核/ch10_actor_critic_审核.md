# 第10章《演员-评论家方法》中文 OCR MD 保真度审核报告

## 概述

- **被审文件**：`强化学习的数学原理_分章/10_第10章_演员-评论家方法.md`
- **真值基准**：英文权威版 `3 - Chapter 10 Actor-Critic Methods.pdf`（pdftotext 提取 `/tmp/rlref_en/ch10.txt`）
- **审核范围**：逐节比对数学公式、定义、定理、算法框、示例、问答；只比数学与事实，不比中文行文优劣；忽略英文提取产生的页码/断行/图表错位噪声。
- **总体结论**：中文 MD 整体结构完整、章节顺序与英文一致，绝大多数公式（基准不变性证明、最优基准、重要性采样、Off-policy 策略梯度定理及证明、确定性策略梯度定理 10.1~10.4 及其方框证明、各算法框）数学内容**与英文真值一致**。仅发现 1 处**明确的 OCR 公式损坏**（式 10.3，严重）、以及若干处中英文实质内容**不一致/缺句**（部分可能是中英文版本差异，已分别标注置信度）。

## 问题清单

| 位置[节/公式] | 类型 | 中文MD现状 | 英文正确形式 | 建议 |
|---|---|---|---|---|
| §10.2.1 式(10.3)（中文 L58-62） | OCR错/公式不符（**严重**） | 右端写作 `… \ln\pi(A\|S,\theta_t)(q_\pi(S,A)-q_\theta)(q_\pi(S,A))`，且 `\tag` 损坏为 `\tag{\(b(S))\],}`，等号右侧出现重复的 `(q_\pi(S,A))`、莫名的 `q_\theta`，并丢失基准 `b(S)` | `E_{S\sim\eta,A\sim\pi}[\nabla_\theta\ln\pi(A\|S,\theta_t)(q_\pi(S,A)-b(S))]`，公式编号应为 `(10.3)` | 重写整条公式：去掉 `q_\theta` 与重复项，把被减项改为 `b(S)`，修复 `\tag{10.3}` |
| §10.4.2 算法10.4 解释第三点（中文 L647） | 漏识（缺句） | "它可以是任何探索性策略，也可以是通过给 μ 添加噪声获得的随机策略[75]。"——到此结束 | 英文(L1458-1460)其后还有一句："In this case, µ is also the behavior policy and hence this way is an on-policy implementation."（此时 μ 也是行为策略，因此这种做法是一种 on-policy 的实现） | 补译此句，否则读者会误以为加噪声后仍是 off-policy |
| §10.6 问答 第1问答（中文 L670） | 漏识（缺句，中等置信） | "Actor-Critic算法实际上就是策略梯度方法。任何策略梯度算法在更新策略的同时也需要估计…" | 英文(L1496-1497)在"actually policy gradient methods"后有一句："Sometimes, we use them interchangeably."（有时我们将二者互换使用） | 建议补译；若属中文版有意省略可保留，但与真值有差 |
| §10.6 问答 第4问答（中文 L682） | 公式不符/内容替换（中等置信） | "…就不需要动作样本。**详细解释可以参见正文。**" | 英文(L1517-1519)："…it is not required to sample actions **and hence any policy can be used**. Therefore, the deterministic policy gradient method is off-policy." | 中文丢失了"因此可以使用任何策略"这一关键论据，且替换成"参见正文"。建议补回该论据 |
| §10.1 算法10.1 正文（中文 L38） | 公式编号不符（低置信，疑版本差异） | "Critic对应于式**(8.36)**给出的Sarsa算法" | 英文(L105-106)为式**(8.35)** | 交叉引用编号差异；中英文版本式号体系可能不同，建议核对中文版第8章 Sarsa 实际式号后再定，非数学错误 |

## 复核确认无误的要点（抽样）

- 式(10.4)(10.5)(10.6) 最优基准与方框10.1 推导：与英文逐项一致（含 `\|\nabla_\theta\ln\pi\|^2` 权重、trace 性质 `tr(AB)=tr(BA)`、次优基准 `b^\dagger(s)=v_\pi(s)`）。
- 式(10.7)(10.8) 优势函数 `δ_π=q_π-v_π`、TD 误差近似、算法10.2 三步更新：一致。
- §10.3.1 重要性采样：式(10.9)(10.10)、重要性权重定义、`p_0=0.5/0.5`、`p_1=0.8/0.2`、期望 `0` 与 `0.6`、退化例 `p_1(+1)=1` 收敛到 `0.5`：数值全部正确。
- 定理10.1 及方框10.2 证明（含 `ρ(s)`、`Pr_π`、引理9.2 代入、重要性采样改写）：一致。
- 算法10.3 Actor/Critic 双重重要性权重、eligibility traces [73]：一致。
- 定理10.2/10.3/10.4 及引理10.1，方框10.3/10.4/10.5（含 Kronecker 积矩阵-向量形式、`(I-γP_μ)^{-1}⊗I_m`、平均奖励情形 `d_μ^T P_μ=d_μ^T`、`d_μ^T 1_n=1`）：数学推导逐步一致。
- 算法10.4 TD 误差、Actor/Critic 更新（Critic 不需重要性采样的解释）：一致。
- §10.5 总结、参考文献编号 [69]-[95]（SAC/TRPO/PPO/TD3/MARL/MBRL 等）：一致。

## 问题计数

**合计 5 处**：其中
- OCR 公式损坏（严重）：1（式10.3）
- 漏识/缺句：2（算法10.4 on-policy 句、问答1 互换句）
- 内容替换/公式不符：1（问答4 缺关键论据）
- 编号差异（低置信，疑版本）：1（8.35 vs 8.36）

最高优先级为**式(10.3)的 OCR 损坏**，必须修复。
