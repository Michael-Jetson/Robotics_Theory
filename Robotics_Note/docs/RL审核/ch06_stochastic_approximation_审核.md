# 第6章《随机近似算法》中文 OCR MD 保真度审核报告

## 概述

- **被审文件**：`/home/gpf/Note/SimpleSLAM_Theory/强化学习的数学原理_分章/06_第6章_随机近似算法.md`
- **真值参照**：`/tmp/rlref_en/ch6.txt`（英文权威版，pdftotext 非结构化提取）；细节复核源 PDF `Book-MathFoundationRL-en/3 - Chapter 6 Stochastic Approximation.pdf`
- **审核范围**：逐节比对 6.1–6.6 全部正文、公式、定理、Q&A。
- **总体评价**：中文 MD 结构完整（节顺序与英文一致），核心数学推导（RM 算法、Dvoretzky 定理证明、SGD 证明）基本保真。发现的问题以**公式编号错位**、**少量 OCR 下标丢失**、**个别段落内容压缩/缺失**为主，未发现破坏性的数学错误。
- **最重要问题**：6.4.3 节公式编号整体错位——中文把"确定性表述"一节的两个公式编为 (6.16)、(6.17)，而英文原版该节只有一个编号 (6.16)（指增量更新式），最小化目标式**不编号**。这导致从 6.4.3 起后续对 (6.16) 的引用全部对不上。

## 问题清单

| 位置[节/公式] | 类型 | 中文MD现状 | 英文正确形式 | 建议 |
|---|---|---|---|---|
| 6.4.3 节 公式(6.16)(6.17)（MD 行 556-558、568-570） | 公式不符（编号错位） | 把最小化式 $\min_w J(w)=\frac1n\sum f(w,x_i)$ 编号为 **(6.16)**；把增量更新式 $w_{k+1}=w_k-\alpha_k\nabla_w f(w_k,x_k)$ 编号为 **(6.17)** | 英文原版：最小化式**无编号**；增量更新式编号为 **(6.16)**。原版全章不存在 (6.17) | 删除 (6.16) 标在最小化式上的编号；把增量更新式改为 (6.16)。并核查 6.4.3 正文中"(6.17)中的算法"应改为"(6.16)" |
| 6.4.3 正文（MD 行 572、574、582） | 公式不符（引用错位，连带） | 多处"(6.17)中的算法" | 原版对应"the algorithm in (6.16)" | 随上条一并把 (6.17) 改回 (6.16) |
| 6.4.2 直觉解释（MD 行 454） | OCR错 | $\mathbb{E}_{x_k}[\nabla wf(w_k,x_k)]=\mathbb{E}_X[\nabla wf(w_k,X)]$，`\nabla w` 缺下标（两处） | $\mathbb{E}_{x_k}[\nabla_w f(w_k,x_k)]=\mathbb{E}_X[\nabla_w f(w_k,X)]$ | 补回梯度算子下标 $\nabla_w$（该行出现两次） |
| 6.2.1 条件(b)举例（MD 行 222） | 公式不符 | 例 $a_k=1/(k+1)$ 或 $a_k=\mathbf{10/k}$ | 原版：$a_k=1/(k+1)$ 或 $a_k=c_k/k$，其中 $c_k$ 有界（更一般的形式） | 建议改为一般形式 $c_k/k$（$c_k$ 有界）以与原意一致；若保留 $10/k$ 需知其只是特例 |
| 6.4.4 节 MBGD 收敛速度段（MD 行 604） | 漏识（段落压缩） | 仅一句"与SGD相比，MBGD的随机性较小，收敛速度通常更快，因为它使用的样本比SGD更多" | 原版另有独立一段：MBGD 比 SGD 快是因 SGD 用 $\nabla_w f(w_k,x_k)$ 近似真实梯度，而 MBGD 用 $(1/m)\sum_{j\in I_k}\nabla_w f(w_k,x_j)$ 更接近真实梯度（随机性被平均掉）；且"MBGD 的收敛性可仿照 SGD 情形证明" | 补译该段（averaged-out randomness + 收敛性可类比 SGD 证明） |
| 6.2.1 节末（MD 行 224 段） | 漏识（句子缺失） | 段落以"应对时变系统"收尾，未提及 $g(x)=x^3-5$ | 原版该处末句："$g(x)=x^3-5$（图6.3 例子）不满足第一个条件，但若初值选取得当（非任意），RM 算法仍能找到根" | 核实是否为版本差异；若按英文真值，应补该句（中文在 6.2 引言行 107/158 已部分提及，可酌情） |
| 6.6 Q&A "为何常讨论期望值估计"（MD 行 724） | 公式不符（事实改写） | "…而因为估计状态或动作值是强化学习中的重要问题，所以我们经常讨论期望值估计问题" | 原版第二句："第7章的时序差分学习算法与用于期望值估计的随机近似算法相似" | 建议按原版改回第二句（强调 TD 与均值估计 SA 的相似性），现中文改成了另一层理由 |
| 6.2.1 收敛直觉 第二条（MD 行 134） | OCR错（轻微） | "如果 $a_{k}$ 足够小" | 原版："If $\lvert a_k g(w_k)\rvert$ is sufficiently small"（第一条为 $a_k g(w_k)$） | 严格应为 $\lvert a_k g(w_k)\rvert$ 足够小；中文简化为 $a_k$ 足够小，含义可接受但与原版措辞不符 |

## 说明（非问题项 / 存疑项）

- 英文 6.4.1 末未显式给出 (6.4) 等于 SGD 的编号差异——中文 6.4.1 与英文一致，无误。
- 中文 6.2.1 行 224 末尾关于"$a_k$ 恒为正常数仍可收敛 [24, 1.5 节]"与英文一致。
- Dvoretzky 定理（6.2）、推广定理（6.3）、其证明（6.3.1–6.3.3）、SGD 收敛证明（方框6.1）逐式核对，数学符号、上下标、条件 $(a)(b)(c)$ 均与英文真值吻合，无误。
- 公式 (6.1)–(6.15) 编号与内容均与英文一致。
- 英文提取中的页码/页眉/图注错位（如 "Fig. 1: xx"）已按要求忽略，不计为问题。
- "10/k"（行222）与"为何讨论期望值估计"（行724）两处也可能是中文版自身的编辑/翻译选择而非 OCR；但以英文为真值口径，仍列出。

## 问题计数

**共 8 条**（其中：公式编号错位/引用错位 2 条、OCR 错 2 条、内容漏识/压缩 2 条、事实改写/特例化 2 条）。

- 高优先级：6.4.3 公式编号错位（(6.16)/(6.17)）及其连带引用——影响交叉引用正确性。
- 中优先级：6.4.2 行454 `\nabla_w` 下标丢失；6.4.4 MBGD 收敛速度段落缺失。
- 低优先级：其余措辞/特例化差异。
