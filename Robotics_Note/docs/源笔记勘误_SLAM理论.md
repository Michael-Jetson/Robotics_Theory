# 源笔记勘误：`/home/gpf/下载/SLAM理论.md`

> 该文件 = 用户个人 SLAM 笔记（3168 行，覆盖十四讲 ch1–7 + 激光 + VIO + ESKF + 仿真）。
> 转换并入《机器人学笔记》前，下列问题须修正/重建。这是会话中通读检出的结果，单独落盘以防压缩丢失。

## A. 数学错误（须改）

| 行 | 问题 | 正确 |
|---|---|---|
| 217–219 | 称反对称阵 $(\mathbf a^\wedge)^{-1}=(\mathbf a^\wedge)^T$ | 反对称阵**奇异不可逆**；正确是 $(\mathbf a^\wedge)^T=-\mathbf a^\wedge$（误把正交阵性质安上）|
| 655 | 雅可比恒等式中间项 $[Z,[Y,X]]$ | 应 $[Z,[X,Y]]$。**注：十四讲原书（04章 L203）是对的，是用户抄错** |
| 1059–1060 | $u=f_x x_d+c_x;\ v=f_x y_d+y_x$ | 第二式 $f_x\!\to\!f_y$、$y_x\!\to\!c_y$ |
| 1084 | 除法畸变 $k_2^4,\ k_3^6$ | 应 $k_2r^4,\ k_3r^6$（漏 r）|
| 1106 | PnP “12 未知数”又说 6 自由度自相矛盾；“6 方程→4 点”推理乱（6 方程=3 点）；ICP 误列为 PnP 解法 | P3P=3点+1消歧=4点；澄清自由度=6；ICP 是点云配准非 PnP |
| 1179 / 1195 | “单**位**矩阵 H” | 应“单**应**矩阵”(homography) |
| 460 / 464 | $Z_c=T_{cw}P_w+w$、$e=z-Tp$ 维度不符（T 4×4 × P 3维）| 须齐次坐标 |

## B. 乱码 / 损坏（粘贴错误，须重写）

| 行 | 问题 |
|---|---|
| 580–588 | **单位四元数群整段乱码**（双重渲染 + 字面反斜杠，Action/Group/Manifold 三条目全坏）|
| 895 | `$$\(X\_w,Y\_w,Z\_w\)$$` 转义泄漏，且与 860 行重复 |
| 1532–1535 | **对极几何本质矩阵关键式乱码** + 空 `$$` |

## C. 残缺 / 未完成

| 行 | 问题 |
|---|---|
| 286–291 | 左乘/右乘表格残缺（空单元格/空公式/主被动未填全）|
| 966 / 1066 | “如果相机的成像是” / “鱼眼…其调整的是” 句子中断 |
| 2156 | $f:F\subseteq\mathbb R^n$ 缺值域（应 $\to\mathbb R$）|

## D. 小瑕疵（转换时静默修）

- 记号混用 $\boldsymbol\eta$/$\mathbf n$ 表同一噪声（ESKF 段）。
- L423 $s_a$ 应通用 $s$。
- L631 “李代数 = 切空间”元素与空间混用（$\phi^\wedge$ 是元素，非切空间本身）。
- L822 代码 `Eigen::Vector3d(0.1.0)` 应 `(0,1,0)`；Sophus 构造/`checkout a621ff` 截断。
- L1346 IMU 字段 `Linear_velocity` 应 `linear_acceleration`（sensor_msgs/Imu 为线加速度）。
- L1508 代码大小写不一致（`_lasercloud`/`diffx` vs `_laserCloud`/`diffX`）。
- L496 “李群=砍掉性质的线性空间” 类比不严谨，正式化时改述。
- L2177/2194 Hessian/Jacobian 矩阵下标维度标错。

## E. 结构 / 范围（关键）

- **后半大量公式是 feishu 鉴权图片**（`internal-api-drive-stream.feishu.cn/...authcode`），**看不到内容、LaTeX 不可渲染**：涉及 IMU 测量/运动模型、点云残差、LOAM 曲率、**FAST-LIO 全部数学**、ESKF 部分、非线性优化（高斯牛顿/LM 整段）、最小二乘引出、MAP/MLE 等。
  → 决策：**按权威文献重建**（高翔《十四讲》/ Solà micro-Lie `1812.01537` / FAST-LIO 原论文），每处打 `\rebuilt` 批注待用户核对。
- 末尾 Docker 安装 / ROS 导航小车仿真（ROS1/ROS2）= 工程命令非理论 → **已定纳入正文**，归「工程实践」部。
- **规划、控制两部本笔记未覆盖**（等后续笔记/论文补）。

## F. 可文本提取的理论段（无须重建，直接用）

刚体运动、李群李代数、相机模型/标定（张正友法、畸变）、对极几何文字、**ESKF 推导（1754–2074 文本完整，质量好）**、非线性优化文字定义——这些是 LaTeX 文本，可直接吸收（仍按规范复核）。
